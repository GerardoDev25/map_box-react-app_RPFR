import { BtnMyLocation, MapView, ReactLogo, SearchBar } from '../components';

export const HomeScreen = () => {
  return (
    <div>
      <BtnMyLocation />
      <MapView />
      <ReactLogo />
      <SearchBar />
    </div>
  );
};
